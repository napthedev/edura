{
  "name": "edura",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:native": "pnpm --filter native dev",
    "dev:web": "pnpm --filter web dev",
    "db:push": "pnpm --filter @edura/db db:push",
    "db:studio": "pnpm --filter @edura/db db:studio",
    "db:generate": "pnpm --filter @edura/db db:generate",
    "db:migrate": "pnpm --filter @edura/db db:migrate"
  },
  "packageManager": "pnpm@10.18.0",
  "dependencies": {
    "dotenv": "^17.2.2",
    "js-cookie": "^3.0.5",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/js-cookie": "^3.0.6",
    "@types/node": "^22.13.11",
    "tsdown": "^0.15.5"
  }
}
